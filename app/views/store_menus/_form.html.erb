<%= form_for @store_menu, html: { multipart: true } do |f| %>
  <% if store_menu.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(store_menu.errors.count, "error") %> prohibited this store_menu from being saved:</h2>

      <ul>
      <% store_menu.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :store_id %>
    <%= f.text_field :store_id %>
  </div>

  <div class="field">
    <%= f.label :image %>
    <%= file_field_tag :image %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
